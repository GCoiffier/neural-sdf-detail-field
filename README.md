# Detail field for neural implicit surfaces

# Usual pipeline

`python train_hkr.py <geometry>`

or `python train_wrapping.py <geometry>`

train_hkr runs the algorithm from Coiffier and Béthune (2025)

train_wrapping runs our point wrapping algorithm.

